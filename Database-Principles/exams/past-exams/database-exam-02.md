# 河南大学计算机与信息工程学院第二学期期末考试 数据库系统原理试卷B卷

### 一、选择题

1. 在数据库技术中，独立于计算机系统的模型是 ( )

* A. E-R 模型
* B. 层次模型
* C. 关系模型
* D. 面向对象的模型

2. 数据库系统中的外模式/模式映像保证了数据的 ( )

* A. 完整性
* B. 逻辑独立性
* C. 并发性
* D. 物理独立性

3. 取出关系中的某些列，并消去重复的元组的关系运算称为 ( )

* A. 取列运算
* B. 投影运算
* C. 联结运算
* D. 选择运算

4. SQL 基本表的创建中是通过 ( ) 实现参照完整性规则的。

* A. 主键子句
* B. 外键子句
* C. 检查子句
* D. NOT NULL

5. 下列SQL语句中，可以对表中数据进行修改的是 ( )

* A. UPDATE
* B. CREATE
* C. ALTER
* D. MODIFY STRUCTURE

6. 下列语句中哪些可以用来对其他用户回收权限 ( )

* A. GIVE
* B. REVOKE
* C. GRANT
* D. CREATE ROLE

7. 语句“DELETE FROM S”的含义是 ( )

* A. 删除S表
* B. 删除S表中的所有记录
* C. 删除基本表S中的列数据
* D. 该语句错误

8. 数据库中 ( ) 是指数据的正确性和相容性

* A. 安全性
* B. 完整性
* C. 并发性
* D. 恢复性

9. 关系数据库规范化是为解决关系数据库中 ( ) 问题而引入的。

* A. 插入异常、删除异常和数据冗余
* B. 提高查询速度
* C. 减少数据操作的复杂性
* D. 保证

10. 对于关系R，如果R的每个非主属性完全函数依赖于码，则R为第几范式 ( )

* A. 4NF
* B. 2NF
* C. 3NF
* D. BCNF

11. 从E-R图向关系模型转换时，一个n:m的联系转换为关系模式时，该关系模式的关键字是 ( )。

* A. n端实体关键字和m端实体关键字的组合
* B. n端实体的关键字
* C. m端实体的关键字
* D. 重新选取其它属性

12. 关于“死锁”，下列说法中正确的是 ( )

* A. 死锁是操作系统中的问题，数据库操作中不存在
* B. 在数据库中防止死锁的方法是禁止两个用户同时操作数据库
* C. 只有出现并发操作时，才有可能出现死锁
* D. 当两个用户竞争相同资源时，不会发生死锁

13. 假如采用关系数据库系统来实现应用，在数据库设计的 ( ) 阶段，需要将E-R 模型转换为关系数据模型。

* A. 概念设计
* B. 物理设计
* C. 逻辑设计
* D. 运行阶段

14. 事务的原子性是指 ( )

* A. 事务中包括的所有操作要么都做，要么都不做
* B. 一个事务内部的操作及使用的数据对并发的其他事务是隔离的
* C. 事务一旦提交，对数据库的改变是永久的
* D. 事务必须是使数据库从一个一致性状态变到另一个一致性状态

15. 数据库中封锁机制是 ( ) 的主要方法

* A. 完整性控制
* B. 安全性控制
* C. 并发控制
* D. 恢复

### 二、填空题

1. 数据库系统中常用的三种数据模型有层次模型、网状模型和 ______。
2. 关系模式的三类完整性约束条件分别是 ______、参照完整性约束和用户完整性约束。
3. 聚合函数“count(列名)”的语义是 ______。
4. 对于属性X的每个具体值，属性 ______ 都有唯一的具体值与之对应，称 ______，或称X决定Y。
5. 数据库中插入一条记录所用的语句是 ______。
6. 并发控制带来的三种数据不一致是 ______、不可重复读和读“脏”数据。
7. 在一个关系R中，若每个数据项都是不可再分割的，那么一定属于 ______。
8. 最基本的封锁类型有两种：排它锁和 ______。
9. 把数据库从错误状态恢复到某一已知的正确状态（亦称为一致状态）的功能，这就是 ______。
10. 实体联系模型中三种联系是一对一联系(1:1)、一对多联系(1:n)、______。

### 三、简答题

1. 有如下两个关系 Student 和 SC，请依次写出  和  的结果。

**Student 表:**

| sno | age | name |
| :-- | :-- | :--- |
| 1   | 18  | ZS   |
| 2   | 20  | ls   |
| 3   | 16  | ww   |
|     |     |      |

**SC 表:**

| sno | cno | grade |
| :-- | :-- | :---- |
| 1   | 2   | 90    |
| 3   | 4   | 85    |

2. 试述关系模型的参照完整性规则。(5分)
3. 简述数据库设计过程。(5分)
4. 数据库的完整性概念与数据库的安全性概念有什么区别和联系？(5分)
5. 现有如下关系模式 教师(教师编号, 姓名, 所在部门, 图书编号, 图书名称, 借期, 还期, 备注)，试回答下列问题:

* (1) 找出关系模式教师的候选码。(2分)
* (2) 该关系模式中是否存在部分函数依赖？如果存在，请写出其中两个。(3分)

### 四、编程题

**现有一个名为“学生成绩管理系统”的数据库，有学生表、课程表、选课表，表结构如下所示：**

* **学生表：** 学号 姓名 性别 出生日期 系别
* **课程表：** 课程号 课程名 学分 学期
* **选课表：** 学号 课程号 成绩

**请用SQL 语言完成如下的操作：**

1. 创建“计算机系”学生视图，该视图有属性学号、姓名、课程号、成绩组成。
2. 查询所有同学的选课及成绩情况，要求显示学号、姓名、课程号和成绩。
3. 插入学号为99103，姓名为“李华”的男学生记录。
4. 将学号为99010的学生姓名改为“王雨”。
5. 删除姓“王”的学生记录。
