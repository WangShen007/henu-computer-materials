# 河南大学计算机与信息工程学院第二学期期末考试 《数据库原理》试卷 B 卷

### 一、单项选择题 (本题共 10 题，每题 2 分，共 20 分)

**1. 数据库系统与文件系统的主要区别是**

* A. 数据库系统复杂，而文件系统简单。
* B. 文件系统不能解决数据冗余和数据独立性问题，而数据库系统可解决。
* C. 文件系统只能管理程序文件，而数据库系统能够管理各种类型的文件。
* D. 文件系统管理的数据量较少，而数据库系统可以管理庞大的数据量。

**2. 三个模式之间存在映射关系是**

* A. 外模式/内模式
* B. 用户模式/存储模式
* C. 外模式/模式
* D. 内模式/外模式

**3. 1 级封锁协议加上对 T 要读取的数据 R 加 S 锁，直到事务结束才释放，属于**

* A. 4 级封锁协议
* B. 1 级封锁协议
* C. 2 级封锁协议
* D. 3 级封锁协议

**4. 当关系模式 R(A,B) 已属于 3NF，下列说法中正确的是**

* A. 它一定消除了插入和删除异常
* B. 仍可能存在插入和删除异常
* C. 一定属于 BCNF
* D. A 和 C 都是

**5. 设有两个事务 T1 和 T2，其并发操作如图所示，下面评价正确的是**

| T1        | T2        |
| :-------- | :-------- |
| 读 A=10   | 读 A=10   |
| A:=A-1    |           |
| 写回 A=15 | A:=A-1    |
|           | 写回 A=15 |

* A. 该操作丢失修改
* B. 该操作不存在问题
* C. 该操作不能重复读
* D. 该操作读“脏”数据

**6. DBMS 中的检查点机制用于**

* A. 检查事务的故障
* B. 减少故障恢复的时间
* C. 检查数据库一致性状态
* D. 检查数据库的完整性

**7. 在数据库设计中，用 E-R 图来描述信息结构但不涉及信息在计算机中的表示，它是数据库设计的____阶段。**

* A. 需求分析
* B. 逻辑设计
* C. 概念设计
* D. 物理设计

**8. 正确的论述是**

* A. 自然连接和等值连接很抽象，但实际上他们是不同的。自然连接要去掉了重复的属性，等值连接不需要去掉重复的属性。
* B. 一个关系是参照关系，那么就不会是被参照关系。
* C. 数据库中有一些 sys 开头的系统表，用来记录 SQL Server 组件、对象所需要的数据，这些系统表全部存放在系统数据库中。
* D. 授予用户 Jean 可以查询帐户表的权限，使用的 SQL 语句是 `GRANT SELECT ON Jean TO 帐户`

**9. 现有关系：学生（学号，姓名，课程号，系号，系名，成绩），为消除数据冗余，至少需要分解为**

* A. 1 个表
* B. 2 个表
* C. 3 个表
* D. 4 个表

**10. 根据关系数据库基于的数据模型——关系模型的特征判断下列正确的一项**

* A. 只存在一对多的实体关系,以图形方式来表示。
* B. 关系模型数据库是数据库发展的最初阶段。
* C. 能体现一对多,多对多的关系,但不能体现一对一的关系。
* D. 以二维表格结构来保存数据，在关系表中不允许有重复行存在。

### 二、填空题 (本题共 10 空，每空 1 分，共 10 分)

1. 每个 SQL Server 都有两类数据库________和________。
2. 根据索引的顺序与数据表的物理顺序是否相同，可以把索引分为两类型________和________。
3. 事务必须具有的四个性质是：原子性、一致性、________和持久性。
4. 数据字典中的________是不可再分的数据单位。
5. 数据库恢复时，系统对已提交的事务要进行________处理。
6. 在数据库并发控制中，两个或更多的事务同时处于相互等待状态，称为________。
7. 数据模型通常由三部分组成，它们是________、数据操作和完整性约束。
8. SQL Server 是一种支持________体系结构的关系数据库管理系统。

### 三、简答题 (本题共 6 题，每题 10 分，共 60 分)

**1. 什么是程序与数据的独立性，数据库要实现哪两种程序与数据的独立性。（本题 10 分）**

**2. 试述关系模型的 3 个组成部分（本题 10 分）**

**3. 简述基本表和视图的区别和联系（本题 10 分）**

**4. 简述关系模型的完整性和安全性区别和联系（本题 10 分）**

**5. 简述数据库设计的步骤和任务（本题 10 分）**

**6. 简述数据库恢复的基本技术和封锁的基本类型有哪些？（本题 10 分）**

### 四、应用题 (本题 10 分)

**设数据库表定义如下：**

**学生表 S**

| 字段名称 | 类型     | 意义 | 备注 |
| :------- | :------- | :--- | :--- |
| SN       | CHAR(3)  | 学号 | 主键 |
| SNM      | CHAR(10) | 姓名 |      |
| DEPT     | CHAR(20) | 系别 |      |

**课程表 C**

| 字段名称 | 类型     | 意义 | 备注 |
| :------- | :------- | :--- | :--- |
| CN       | CHAR(3)  | 课号 | 主键 |
| CNM      | CHAR(10) | 课名 |      |
| T        | INT      | 课时 |      |

**选课表 X**

| 字段名称 | 类型    | 意义 | 备注               |
| :------- | :------ | :--- | :----------------- |
| SNO      | CHAR(3) | 学号 | 主键，参照表 S(SN) |
| CNO      | CHAR(3) | 课号 | 主键，参照表 C(CN) |
| G        | INT     | 成绩 |                    |

**1. 编写 SQL 语句：查询计算机系的学生学号和姓名，结果按照姓名倒序排列（本题 5 分）**

**2. 编写 SQL 语句：查询选择课号为 ‘123’ 的学生姓名和成绩（本题 5 分）**
