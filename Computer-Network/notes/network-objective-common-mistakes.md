1.家庭通过电话线上网采用的技术：xDSL

2.数据链路层使用网桥连接两个以太网，碰撞域保持不变，广播域合二为一。

3.传统以太网的10BASE-F标准中使用的传输媒体是光纤。

 传统以太网的10BASE-T标准中使用的传输媒体是无屏蔽双绞线（UTP）。

4.ICMP可用来报告由于数据包交付问题而产生的错误。

  PING指令直接使用网络层的ICMP协议来测试两个主机之间的连通性。（PING使用了ICMP中的回送请求与回送回答报文）（PING是应用层直接使用网络层ICMP的一个例子，没有通过运输层的UDP或者ＴＣＰ）

5.IGMP可以实现多播通信。

6.RIP使用UDP，OSPF使用IP，BGP使用TCP。

7.内部网关协议（RIP和OSPF）寻求最佳路由

外部网关协议BGP力求寻找一条能够到达目的网络且比较好的路由，并非最佳。

8.流量控制用来防止：接收方缓冲区溢出

  拥塞控制用来防止：过多的数据注入网络（课本原话）

9.网桥根据数据帧中的源MAC地址建立转发表。

 网桥根据数据帧中的目的MAC地址查找转发表，转发数据帧。

10.FTP协议的服务器端，控制连接的默认端口号是21，数据传送连接是20；

   HTTP协议的服务器默认端口号是80

11.如果运输层向应用层提供面向连接的服务，网络层不一定向运输层提供面向连接的服务。比如：TCP面向连接，IP无连接。

12.计算机网络向用户提供的最重要的两个功能是连通性和资源共享。（互联网的两个重要基本特点）

13.在连续ARQ协议中，当用ｎ个比特进行编号时，若接收窗口的大小为１，则发送窗口的数值为：ｗ＜＝２＾ｎ－１

14.三报文握手的SYN=1的报文段要消耗掉一个序号。

15.运输层提供端到端的服务。

16.数据链路层三个基本问题：封装成帧、透明传输、差错检测

17.协议的三要素：语法、语义、同步

18.物理层的四个特性：机械特性、电气特性、功能特性、过程特性。

19.路由器分组转发部分由三部分组成：一组输入端口、交换结构、一组输出端口

20.以太网采用的共享信道技术是随机接入。

21.网络层提供的两种服务：面向连接的虚电路服务、无连接的数据报服务。

22.因特网用户计算机和某个ISP连接才能接入因特网，他们通信时所使用的数据链路层协议是PPP。

23.使用套接字可以唯一表示互联网络中通信的一端；（每一条TCP连接唯一的被通信的两个套接字所确定）；套接字格式为：{（IP1：port1），（IP2：port2）}。

24.TCP连接建立过程中的最后一次确认是为了防止已失效的连接请求报文突然又到达服务器端而产生错误。

25.计算机网络向用户提供的最重要的两个功能是连通性和资源共享。

26.根据域名服务器所起的作用，可以将其划分成四种不同类型：根域名服务器、顶级域名服务器、权限域名服务器、本地域名服务器。

27.完整的统一资源定位符URL由四部分组成：协议、主机、端口、路径。

28.传输媒体可分为两大类：导引型和非导引型传输媒体。

29.根据信息中代表信息的参数取值方式不同，信号可分为模拟信号和数字信号。

比较偏的传输媒体介质：

1.若一条双绞线的两端都是EIA/TIA568-B的标准，则此双绞线可用来连接主机和交换机。
