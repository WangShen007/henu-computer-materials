# 教师复习题

---

### 一、选择题

1. 完整的计算机系统应包括 [ ]。
   * A. 运算器、存储器和控制器
   * B. 外部设备和主机
   * C. 主机和实用程序
   * D. 配套 of 硬件设备和软件系统

2. 计算机系统中的存储器系统是指 [ ]。
   * A. RAM 存储器
   * B. ROM 存储器
   * C. 主存储器
   * D. 主存储器和外存储器

3. 冯·诺依曼机工作方式的基本特点是 [ ]。
   * A. 多指令流单数据流
   * B. 按地址访问并顺序执行指令
   * C. 堆栈操作
   * D. 存储器按内部选择地址

4. CPU 中不包括 [ ]。
   * A. 操作码译码器
   * B. 指令寄存器
   * C. 地址译码器
   * D. 通用寄存器

5. 在全相联映射、直接映射 and 组相联映射中，块冲突概率最小的是 [ ]。
   * A. 全相联映射
   * B. 直接映射
   * C. 组相联映射
   * D. 不确定

6. 下列数中最大的为 [ ]。
   * A. $(101001)_2$
   * B. $(2000)_3$
   * C. $(52)_7$
   * D. $(2E)_{16}$

7. 计算机操作的最小单位时间是 [ ]。
   * A. 时钟频率
   * B. 指令周期
   * C. CPU 周期
   * D. 中断周期

8. 在下列数中最小的数为 [ ]。
   * A. $(101001)_2$
   * B. $(52)_8$
   * C. $(101001)_{\text{BCD}}$
   * D. $(233)_{16}$

9. 在下列数中最大的数为 [ ]。
   * A. $(10010101)_2$
   * B. $(227)_8$
   * C. $(143)_5$
   * D. $(96)_{16}$

10. 在机器中，[ ] 的零的表示形式是唯一的。
    * A. 原码
    * B. 补码
    * C. 反码
    * D. 原码和反码

11. 在下列几种存储器中，CPU 不能直接访问的是 [ ]。
    * A. 硬盘
    * B. 内存
    * C. Cache
    * D. 寄存器

12. 计算机的存储系统是指 [ ]。
    * A. RAM
    * B. ROM
    * C. 主存储器
    * D. Cache、主存储器和外存储器

13. 假设变址寄存器 $R$ 的内容为 $1000\text{H}$，指令中的形式地址为 $2000\text{H}$；地址 $1000\text{H}$ 中的内容为 $2000\text{H}$，地址 $2000\text{H}$ 中的内容为 $3000\text{H}$，地址 $3000\text{H}$ 中的内容为 $4000\text{H}$，则变址寻址方式下访问到的操作数是 [ ]。
    * A. $1000\text{H}$
    * B. $2000\text{H}$
    * C. $3000\text{H}$
    * D. $4000\text{H}$

14. 指令中地址码给出的是操作数的有效地址，则该指令采用 [ ]。
    * A. 直接寻址
    * B. 立即寻址
    * C. 寄存器寻址
    * D. 间接寻址

15. 微程序控制存储器属于 [ ] 的一部分。
    * A. 主存
    * B. 外存
    * C. CPU
    * D. 缓存

16. 在一个 16 位的总线系统中，若时钟频率为 $100\text{MHz}$，总线周期为 5 个时钟周期传输一个字，则总线带宽是 [ ]。
    * A. $4\text{MB/s}$
    * B. $40\text{MB/s}$
    * C. $16\text{MB/s}$
    * D. $64\text{MB/s}$

17. 计算机系统中采用补码运算的目的是为了 [ ]。
    * A. 与手工运算方式保持一致
    * B. 提高运算速度
    * C. 简化计算机的设计
    * D. 提高运算的精度

---

### 二、填空题

1. 若某数 $x$ 的真值为 $-0.1010$，在计算机中该数表示为 $1.0110$，则该数所用的编码方法是 __________________ 码。
2. 长度相同但格式不同的 2 种浮点数，假定前者阶码长、尾数短，后者阶码短、尾数长，其他规定均相同，则它们可表示的数的范围和精度为 __________________。
3. 某机字长 32 位，采用定点小数表示，符号位为 1 位，尾数为 31 位，则可表示的最大正小数为 __________________，最小负小数为 __________________。
4. 运算器虽由许多部件组成，但核心部分是 __________________。
5. 在定点二进制运算器中，减法运算一般通过 __________________ 来实现。
6. 在定点运算器中，无论采用双符号位还是单符号位，必须有 __________________，它一般用 __________________ 来实现。
7. 在计算机内部将两个 __________________ 相加可能产生溢出。
8. 在定点数运算中产生溢出的原因是 __________________。
9. 下溢指的是 __________________。
10. 存储单元是指 __________________。

---

### 三、计算与简答题

1. 设机器字长 32 位，定点表示，尾数 31 位，数符 1 位，问：
   * (1) 定点原码整数表示时，最大正数是多少？最大负数是多少？
   * (2) 定点原码小数表示时，最大正数是多少？最大负数是多少？

2. 现有 $1024 \times 1$ 的存储芯片，若用它组成容量为 $16\text{K} \times 8$ 的存储器。试求：
   * (1) 实现该存储器所需的芯片数量？
   * (2) 若将这些芯片分装在若干个块板上，每块板的容量为 $4\text{K} \times 8$，该存储器所需的地址线总位数是多少？其中几位用于选板？几位用于选片？几位用作片内地址？

3. 什么是 I/O 组织方式？有哪几种 I/O 组织方式？各自的特点是什么？

4. CPU 的地址总线 16 根 ($A_{15} \sim A_0$，$A_0$ 是低位)，双向数据总线 16 根 ($D_{15} \sim D_0$)，控制总线中与主存有关的信号有 $\overline{MREQ}$ (允许访存，低电平有效)，$R/\overline{W}$ (高电平读命令，低电平写命令)。主存地址空间分配如下：$0 \sim 8191$ 为系统程序区，由 EPROM 芯片组成；从 $8192$ 起一共 $32\text{K}$ 地址空间为用户程序区；最后 (最大地址) $4\text{K}$ 地址空间为系统程序工作区。上述地址为十进制，按字编址。现有如下芯片：
   * EPROM：$8\text{K} \times 16$ 位 (控制端仅有 $\overline{CS}$)
   * SRAM：$16\text{K} \times 1$ 位，$2\text{K} \times 8$ 位，$4\text{K} \times 16$ 位，$8\text{K} \times 16$ 位
   请画出 CPU 与存储器的连接图，要求：
   * (1) 指出选用的存储芯片类型及数量；
   * (2) 详细画出片选逻辑。

   *地址分配参考图：*
   ```
   +-----------------------+ 65535
   |     4K SRAM (工作区)   |
   +-----------------------+ 61440
   |        空 闲 区        |
   +-----------------------+ 40960
   |    32K SRAM (用户区)   |
   +-----------------------+ 8192
   |     8K EPROM (系统区)  |
   +-----------------------+ 0
   ```

5. 用一台 $50\text{MHz}$ 处理机执行某标准测试程序，它包含的混合指令数和相应所需的平均时钟周期数如下表所示：

   | 指令类型 | 指令数目 | 平均时钟周期数 |
   | :---: | :---: | :---: |
   | 整数运算 | 45000 | 1 |
   | 数据传送 | 32000 | 2 |
   | 浮点运算 | 15000 | 2 |
   | 控制传送 | 8000 | 5 |

   求有效 CPI、MIPS、处理机程序执行时间 $t_{\text{CPU}}$。

6. 某指令系统中有 32 条单字长 (32 位) 二地址 RS 型指令，已知系统中通用寄存器 16 个，操作数 S 可选用直接寻址、间接寻址、变址寻址、基址寻址、相对寻址、寄存器间接寻址 6 种寻址方式，采用专用的基址和变址寄存器。直接寻址方式中，可直接寻址的存储空间为多大？（写出指令格式并写出推导过程）
