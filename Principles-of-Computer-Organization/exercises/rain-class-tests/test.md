# 计算机组成原理雨课堂测试

## 模拟试卷

### 一、单选题（每题 2 分，共 42 分）

1. 下列有关微程序控制器的说法中，正确的是（    ）
   * A. 微程序控制方式执行指令，要比硬布线控制方式快
   * B. 微程序计数器 $\mu\text{PC}$ 可以代替程序计数器 $\text{PC}$
   * C. 控制存储器中，一个单元存放一条微指令
   * D. 控制存储器一般采用高速 $\text{RAM}$ 来构成

2. 下列部件不属于控制器的是（    ）
   * A. 指令寄存器
   * B. 程序计数器
   * C. 程序状态字寄存器
   * D. 时序电路

3. 某计算机的指令流水线由四个功能段组成，指令流经各功能段的时间（忽略各功能段之间的缓存时间）分别是 $90\text{ ns}$、$80\text{ ns}$、$70\text{ ns}$ 和 $60\text{ ns}$，则该计算机的 CPU 时钟周期至少是（    ）
   * A. $90\text{ ns}$
   * B. $80\text{ ns}$
   * C. $70\text{ ns}$
   * D. $60\text{ ns}$

4. 微程序控制器中的机器指令与微指令的关系是（    ）
   * A. 一条微指令由若干条机器指令组成
   * B. 每一条机器指令由一条微指令来执行
   * C. 一段机器指令组成的程序可由一条微指令来执行
   * D. 每一条机器指令由一段用微指令编成的微程序来解释执行

5. 假设某机器有 80 条指令，平均每条指令有 4 条微指令组成，其中取指微指令是所有指令公用的，已知微指令长度为 32 位，则控制存储器的容量是（    ）
   * A. $80 \times 32\text{ bit}$
   * B. $320 \times 32\text{ bit}$
   * C. $241 \times 32\text{ bit}$
   * D. $249 \times 32\text{ bit}$

6. 分析以下两条指令中，数据相关性的类型（    ）
   ```assembly
   I1    ADD   R3, R4    ; (R3)+(R4)→R3
   I2    MUL   R4, R5    ; (R4)×(R5)→R4
   ```
   * A. 读后写
   * B. 写后读
   * C. 写后写
   * D. 读后读

7. 在微程序控制器中，构成控制信号序列的最小单位是（    ）
   * A. 机器指令
   * B. 微命令
   * C. 微指令
   * D. 微程序

8. 兼容性微命令指几个微命令是（    ）
   * A. 可以相继出现
   * B. 可以同时出现
   * C. 可以相互替代
   * D. 可以相互容错

9. 在微程序控制器中，执行指令微程序的首条微指令地址是通过（    ）得到的。
   * A. 程序计数器（PC）
   * B. 前条微指令
   * C. $\mu\text{PC} + 1$
   * D. 指令操作码映射

10. 某计算机主存空间为 $4\text{ GB}$，字长为 32 位，按字节编址，采用 32 位定长指令字格式，若指令按字边界对齐存放，则程序计数器和指令寄存器的位数至少为（    ）。
    * A. 30、30
    * B. 30、32
    * C. 32、30
    * D. 32、32

11. 指令译码器进行译码的是（    ）。
    * A. 整条指令
    * B. 指令的操作码字段
    * C. 指令的地址
    * D. 指令的操作数字段

12. 某计算机采用微程序控制器，共有 32 条指令，公共的取指令微程序包含 2 条微指令，各指令对应的微程序平均由 4 条微指令组成，采用断定法（下地址字段）确定下条微指令地址，则微指令中下地址字段的位数至少是（    ）。
    * A. 5
    * B. 6
    * C. 8
    * D. 9

13. 单周期处理器中所有指令的指令周期为一个时钟周期。下列关于单周期处理器的叙述中，错误的是（    ）
    * A. 可以采用单总线结构数据通路
    * B. 处理器时钟频率较低
    * C. 在指令执行过程中控制信号不变
    * D. 每条指令的 CPI 为 1

14. 下列关于超标量流水线特性的叙述中，正确的是（    ）
    Ⅰ. 能缩短流水线功能段的处理时间；
    Ⅱ. 能在一个时钟周期内同时发射多条指令；
    Ⅲ. 能结合动态调度技术提高指令执行并行性。
    * A. 仅 Ⅰ
    * B. 仅 Ⅰ、Ⅲ
    * C. 仅 Ⅱ、Ⅲ
    * D. Ⅰ、Ⅱ、Ⅲ

15. 下列部件不属于控制器的是（    ）
    * A. 指令寄存器
    * B. 程序计数器
    * C. 状态字寄存器
    * D. 时序电路

16. CPU 中保存当前正在执行指令的寄存器是（    ）
    * A. 指令寄存器
    * B. 指令译码器
    * C. 数据寄存器
    * D. 地址寄存器

17. 以下关于计算机系统的概念中，正确的是（    ）
    Ⅰ. CPU 不包括地址译码器；
    Ⅱ. CPU 的程序计数器中存放的是操作数地址；
    Ⅲ. CPU 中决定指令执行顺序的是程序计数器；
    Ⅳ. CPU 的状态寄存器对用户是完全透明的。
    * A. Ⅰ、Ⅲ
    * B. Ⅲ、Ⅳ
    * C. Ⅱ、Ⅲ、Ⅳ
    * D. Ⅰ、Ⅲ、Ⅳ

18. 假定不采用 Cache 和指令预取技术，且机器处于“开中断”状态，则在下列有关指令执行的叙述中，错误的是（    ）
    * A. 每个指令周期中 CPU 都至少访问内存一次
    * B. 每个指令周期一定大于或等于一个 CPU 时钟周期
    * C. 空操作指令的指令周期中任何寄存器的内容都不会被改变
    * D. 当前程序在每条指令执行结束时都可能被外部中断打断

19. CPU 响应中断的时间是（    ）
    * A. 一条指令执行结束
    * B. I/O 设备提出中断
    * C. 取指周期结束
    * D. 指令周期结束

20. 通常情况下，一个微程序的周期对应一个（    ）
    * A. 指令周期
    * B. 时钟周期
    * C. CPU 周期
    * D. 总线周期

21. 下面有关控制相关的描述中，错误的是（    ）
    * A. 条件转移指令可能引起控制相关
    * B. 在分支指令加入若干空操作可以避免控制冒险
    * C. 采用转发（旁路）技术，可以解决部分控制相关
    * D. 通过编译器调整指令执行顺序可以解决部分控制相关

### 二、填空题（共 3 分）

22. （习题 5.12）今有 4 级流水线，分别完成取指、指令译码并取数、运算、送结果四步操作。假设完成各步操作的时间依次为 $100\text{ ns}$、$100\text{ ns}$、$80\text{ ns}$、$50\text{ ns}$。请问：
    * （1）流水线的操作周期应设计为 ______ $\text{ns}$。
    * （2）若相邻两条指令发生数据相关，硬件上不采取措施，那么第 2 条指令至少需要延迟 ______ 个周期。
    * （3）如果在硬件设计上采用专用通路技术，当相邻两条指令发生数据相关时，第 2 条指令至少需要延迟 ______ 个周期。

---

## 参考答案

### 一、单选题答案

| 题号 | 答案 | 题号 | 答案 | 题号 | 答案 | 题号 | 答案 |
| :--: | :--: | :--: | :--: | :--: | :--: | :--: | :--: |
|  1   |  C   |  2   |  C   |  3   |  A   |  4   |  D   |
|  5   |  C   |  6   |  A   |  7   |  B   |  8   |  B   |
|  9   |  D   |  10  |  B   |  11  |  B   |  12  |  C   |
|  13  |  A   |  14  |  C   |  15  |  C   |  16  |  A   |
|  17  |  A   |  18  |  C   |  19  |  A   |  20  |  A   |
|  21  |  C   |      |      |      |      |      |      |

### 二、填空题答案

22. 
    * （1）$100\text{ ns}$
    * （2）2 个周期
    * （3）1 个周期