###### 01

```csharp
下列变量定义中，不正确的是（D）
int a, b=3;   //在cs上运行会报错
float f=3.5f;
char ch=‘A’;
enum MyColor:byte{Red=0, Green=254, Blue, Yellow};
```

你没初始化 不代表你没定义

`拓展`:在c语言里,未初始化的全局变量和未初始化的局部变量的值(涉及:计算机系统基础,`链接`部分)
`局部变量`:函数返回在pop出当前栈帧的时候，并不会清理它遗留在栈帧里的数据，下个函数调用再次重用到该栈帧的内存时，未初始化的局部变量将会被遗留数据影响，从而变得不确定(脏值)！(https://zhuanlan.zhihu.com/p/359775573)
`全局变量`:放在.bss节中,为0