**5. 考虑下面文法 **$G_1$**：**

**$S \rightarrow a \mid \wedge \mid (T)$**

**$T \rightarrow T, S \mid S$**

（1）消去 **$G_1$** 的左递归。然后，对每个非终结符，写出不带回溯的递归子程序。

（2）经改写后的文法是否是 LL(1) 的？给出它的预测分析表。

**6. 对下面的文法 G：**

**$E \rightarrow TE'$
**

**$E' \rightarrow +E \mid \varepsilon$**

**$T \rightarrow FT'$**

**$T' \rightarrow T \mid \varepsilon$**

**$F \rightarrow PF'$**

**$F' \rightarrow *F' \mid \varepsilon$**

**$P \rightarrow (E) \mid a \mid b \mid \wedge$**

（1）构造这个文法的每个非终结符的 FIRST 和 FOLLOW 集合。

（2）证明这个文法是 LL(1) 的。

（3）构造它的预测分析表。

（4）构造它的递归下降分析程序。

**7. 对下面文法：**

**$Expr \rightarrow -Expr$**

**$Expr \rightarrow (Expr) \mid Var \ ExprTail$**

**$ExprTail \rightarrow -Expr \mid \varepsilon$**

**$Var \rightarrow id \ VarTail$**

**$VarTail \rightarrow (Expr) \mid \varepsilon$**

（1）构造 LL(1) 分析表。

（2）给出对句子 **$id--id((id))$** 的分析过程。
