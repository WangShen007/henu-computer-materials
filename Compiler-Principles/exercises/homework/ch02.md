**2.** 令文法 **$G(N)$** 为：

$$
N \rightarrow D \mid ND
$$

$$
D \rightarrow 0 \mid 1 \mid 2 \mid 3 \mid 4 \mid 5 \mid 6 \mid 7 \mid 8 \mid 9
$$

(1) **$G(N)$** 的语言 **$L(G(N))$** 是什么？

(2) 给出句子 0731 和 863 的最左推导和最右推导。

---

**4.** 写一个文法，使其语言是偶数集，且每个偶数不以 0 开头。

---

**5.** 给出下面语言的相应文法。

$$
L_1 = \{a^nb^nc^i \mid n \ge 1, i \ge 0\}
$$

$$
L_2 = \{a^ib^nc^n \mid n \ge 1, i \ge 0\}
$$

$$
L_3 = \{a^nb^na^mb^m \mid n, m \ge 0\}
$$

$$
L_4 = \{1^n0^m1^m0^n \mid n, m \ge 0\}
$$

---

**7.** 令文法 **$G(E)$** 为：

$$
E \rightarrow T \mid E + T \mid E - T
$$

$$
T \rightarrow F \mid T * F \mid T / F
$$

$$
F \rightarrow (E) \mid i
$$

(1) 给出 **$i+i*i$**、**$i*(i+i)$** 的最左推导和最右推导。

(2) 给出 **$i+i+i$**、**$i+i*i$** 和 **$i-i-i$** 的语法树。

---

**10.** 证明下面的文法是二义的：

$$
S \rightarrow iSeS \mid iS \mid i
$$
